# Core GUI
PySide6>=6.5.0

# PDF rendering, text extraction, annotations
PyMuPDF>=1.23.0

# PDF merging, splitting, encryption, metadata
pypdf>=4.0.0

# Image handling
Pillow>=10.0.0

# OCR (also needs the Tesseract binary installed on the system)
pytesseract>=0.3.10

# PDF generation (used for some conversion features)
reportlab>=4.0.0

# Optional: nicer DOCX export. The app falls back gracefully if missing.
python-docx>=1.0.0
numpy>=1.24
openpyxl
